<LinearLayout
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:gravity="center">

    <com.mikeroelens.emojification.view.PixelatedTextView
        android:id="@+id/txtCurrentGameMessage"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:padding="20dp"
        android:text="@string/mid_game_screen_currently_playing_message"
        android:textSize="20sp"/>

    <com.mikeroelens.emojification.view.PixelatedButton
        android:id="@+id/btnStop"
        android:layout_width="225dp"
        android:layout_height="100dp"
        app:textSize="25sp"
        app:text="@string/mid_game_screen_stop"
        app:backgroundColour="@android:color/holo_red_dark"
        app:textColour="@android:color/white"/>

</LinearLayout>